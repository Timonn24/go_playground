Here we do some pets projects and researches using Golang.

* distributed_kv: implements distributed key-value storage based on hashicorp/memberlist implementation of gossip-protocol.
	API: /set/{key}/{value}
	     /get/{key}